extends layout

block content
  h1= title
  p Welcome to #{title}
  p Routes with logic should be added to ./routes/
  p Routes that are raw data should be added to ./public/data/
  p Routes are generated by removing the file extension, and replacing all periods with slashes in the filename. Ex: api.data.all.json --> /api/data/all